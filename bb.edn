{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([omron-sysmac.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'omron-sysmac.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
